import styles from './Contact.module.css';

const Contact = () => {
    return (
        <section className={styles['container-contact']}>
            <h1 className={styles['container-contact-title']}>This is contact page.</h1>
        </section>
    );
}

export default Contact;